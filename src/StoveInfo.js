import React from 'react';

const StoveInfo = ({ result }) => {
    // Render stove information based on 'result'
    // ...

    return (
        <div>
            {/* Render stove information here */}
        </div>
    );
};

export default StoveInfo;
